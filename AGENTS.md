# Math Visualizations 工程规范

本仓库用于制作可交互、可录屏、数学上可信的概念讲解。每个主题放在独立子工程中，但共享本文定义的 Scene 模型、视觉语言、公式排版和验收要求。

本文适用于仓库根目录及所有子目录。子工程可以增加自己的 `AGENTS.md`，但不得降低本文中的数学正确性、KaTeX、录屏布局和验收要求。

## 1. 产品目标

- 先做出可运行的交互原型，再讨论长篇架构。
- 一个画面只讲清一个核心关系；通过多个 Scene 形成完整概念讲解。
- 可视化不是装饰。位置、长度、夹角、方向、颜色和状态都必须对应明确的数学含义。
- 同时提供视觉直觉与可核验状态，例如坐标、函数值、梯度、约束残差、乘子和条件是否成立。
- 默认面向中文数学科普；术语准确，句子简短，优先用图形建立直觉，必要时才给公式。

## 默认构建偏好（从 dual-space 提炼）

以下是后续新建和修改教学画面的默认标准；用户对某一幕的明确要求优先。

- **极简**：纯黑教学画布、大图形、充足留白；只保留表达当前关系必需的对象，避免装饰网格、渐变、光效和多余卡片。
- **不显示标题和说明**：默认不放页面大标题、Scene 标题、副标题、段落说明、图下注释或操作提示。名称与讲解保留在元数据或文档中；只有辨认对象确有需要时才使用短标签。
- **不显示导航栏**：普通模式与录屏模式默认都不展示场景标签栏、步骤按钮、页码或顶部/底部导航；不为它们预留位置。保留键盘导航，普通模式仅保留必要的画布外“全屏录制”入口。
- **能画就不写**：优先用对象、位置、颜色、箭头、轮廓及具体结果表达关系。例如用水果袋经过收银台得到数值的画面表达映射，不再附一段同义说明。
- **少用公式**：先呈现具体对象和变化，只在图形无法准确表达关键关系时加入最少的符号或公式；不默认叠加抽象定义、公理列表和推导。保留的数学表达式仍必须用 KaTeX，数学正确性与数值验证要求不变。
- **渐进展现**：先出现对象，再出现操作或关系，最后出现结果；每一步只新增一个理解要点，避免一次铺满全屏。
- **用动画解释变化**：让对象真实执行交换、合并、缩放、映射或抵消，并保持对象身份和数学语义连续；不要仅靠整页淡入代替过程。动画由方向键触发，完成后停住等待下一步，默认不自动推进教学步骤。过渡以约 1 秒为起点，按过程调整；明确要求静态总览时保持静态。
- **录屏可读**：保持固定 16:9 构图；图标、必要标签和结果足够大，缩小到手机观看仍可辨认。

构建前逐项检查：能删掉的标题和文字是否已删？公式能否换成图？每次变化是否只讲一个关系？动画是否展示了这个关系？画面是否没有可见导航？

## 2. 仓库与子工程边界

- 每个数学主题是独立子工程，例如 `dual-space/`。新项目采用 `templates/scene-project/` 模板，在仓库根目录执行 `node scripts/create-project.mjs <project-name>` 创建。
- `dual-space/` 是交互与视觉参考；`kkt-conditions/` 保留为历史项目，不要求迁移或跟随新架构维护。
- 子工程独立维护 `package.json`、源码、测试和运行命令，不要把多个主题耦合成一个应用。
- 可复用的交互、公式或 Scene 基础设施应抽成子工程内的通用模块；经过至少两个主题验证后，再考虑提升为仓库级公共包。
- 不要提交构建产物、录屏文件或临时运行文件。所有 `*.mp4` 必须被 Git 忽略。
- 使用现有包管理器和锁文件，不要仅为启动服务切换包管理器或生成新的锁文件。

新项目默认采用以下结构（数学和几何目录按需创建）：

```text
<project>/
├── app/                  # 入口、播放器装配与全局样式
├── components/           # ScenePlayer、MathFormula、SvgFormula
├── scenes/               # 教学组件与项目 Scene 注册表
├── lib/
│   ├── scenes/           # Scene 类型、注册表、排序
│   ├── math/             # 纯数学计算与约束判断
│   └── geometry/         # 坐标变换、投影、命中测试
├── public/
├── tests/
├── package.json
└── README.md
```

## 3. Scene 模型

### 3.1 定义

一个 **Scene** 是一个可在单个 1920×1080 屏幕内完整呈现的、自包含的讲解画面。一个概念由多个有序 Scene 组成。

Scene 和 Scene 内的步骤不同：

- Scene 负责一个独立教学目标，例如“等高线与梯度”或“KKT 的力平衡”。
- Scene 内步骤负责逐层揭示同一教学目标，例如“等高线 → 梯度 → 正交关系”。
- 不要为了省事把多个教学目标塞进同一个 Scene，也不要把每一次显隐变化都拆成 Scene。

### 3.2 必需字段

```ts
type SceneDefinition = Readonly<{
  id: string;
  conceptId: string;
  order: number;
  route: string;
  title: string;
  summary: string;
  viewport: Readonly<{ width: 1920; height: 1080 }>;
}>;
```

- `id`：稳定、语义化、仅供工程内部使用，不暴露给观众。创建后不要因排序或文案变化而改名。
- `conceptId`：把同一概念的 Scene 归为一组。
- `order`：控制排序；使用 `10, 20, 30` 等留有插入空间的值。
- `route`：渲染该 Scene 的路由；多个 Scene 可以由同一路由和内部状态承载。
- `title` / `summary`：用于登记教学名称和一句话目标的元数据，默认不渲染到画面。
- `viewport`：固定记录 1920×1080 的设计契约。

新项目将 `stepCount` 和 `component` 与以上元数据共同登记在 `scenes/registry.ts`。`ScenePlayer` 统一管理导航与录屏，Scene 只接收 `step` 并负责教学内容。不得在 App 中另建按索引对应的步骤数数组或组件条件链。模板默认每个 Scene 为 1–9 步，对应数字键直接跳转。

注册表必须检查：

- `id` 全局唯一；
- 同一 `conceptId` 下 `order` 唯一；
- 查询结果始终按 `order` 排序；
- 步骤数是 1–9 的整数。

## 4. Scene 导航与录屏

- 页面必须提供明显的“全屏录制”入口。
- 录屏模式按 1920×1080 设计：整个 Scene 和需要的状态面板必须完整可见，不允许页面滚动。
- 普通模式和录屏模式均固定 16:9 构图。外部工具栏可以换行，Scene 内部不得响应式重排。
- 模板使用 1440×810 设计坐标，由播放器统一等比缩放；1920×1080 是输出与验收契约。Scene 内避免使用随浏览器改变的 `vw/vh`，不得重复缩放。
- Scene 名称保留在注册表中，默认不显示标题或场景导航。
- 普通模式和录屏模式默认均不显示顶部、底部或画布内导航栏、场景标签、步骤按钮和页码，也不为导航预留空间。
- 录屏模式隐藏外部工具栏与 Scene 标签，通过键盘跨步骤、跨 Scene 切换。
- 键盘导航是必需能力：
  - `ArrowRight` / `PageDown`：下一步骤；当前 Scene 结束后进入下一 Scene。
  - `ArrowLeft` / `PageUp`：上一步骤；当前 Scene 开头时返回上一 Scene。
  - 数字键：跳转当前 Scene 内步骤。
  - `Escape`：退出录屏模式。
- Scene 切换时应保留录屏状态；不要强制退出全屏。
- 键盘事件不得抢占 `input`、`textarea`、`select` 或可编辑元素。

## 5. 视觉语言

默认遵循上述极简构建偏好；以下颜色与排版约定仅用于确有必要的元素：

- 页面背景：简单低对比底色，不增加装饰网格。
- 内容卡片：仅在分组确有需要时使用，不默认添加描边和阴影。
- 数学画布：纯黑背景 `#000`，使曲线、向量和可行域成为视觉主体。
- 标题：衬线中文字体；正文：清晰的无衬线字体；数学：KaTeX。
- 强调色具有稳定语义，不能在同一 Scene 中随意复用：
  - 珊瑚红：下降方向、驱动力、错误或未平衡状态。
  - 黄色：当前选中状态、梯度或正在解释的关键几何对象。
  - 蓝色：约束反力或次要向量。
  - 青色：可行域、约束边界或基础几何参照。
  - 紫色：合力、组合或高层集合。
- 同一数学对象在 2D、3D、图例和状态面板中必须使用同一种颜色。
- 标签贴近所属对象，但不得遮挡箭头、粒子、边界或其他标签。
- 当前状态可高亮，历史/参考状态降低透明度。不要靠大量边框和卡片制造层级。
- 默认观众可能在手机上观看录屏：关键结论、子图标题、图内标签和向量名称必须按缩小后仍可读来设计；不要用只在桌面近距离观看时才看得清的小字。并列子图的标题应以各自子图中心对齐。

## 6. 数学公式与 KaTeX（硬性要求）

- **所有数学表达式必须使用 KaTeX 渲染。** 包括标题中的变量、正文行内公式、坐标轴、SVG 标签、图例、状态指标和按钮中的公式。
- 不得用 Unicode、HTML 下标或普通文本伪装公式，例如 `x₁`、`∇f`、`g(x)<=0`、`z = 3.84` 都应改为 KaTeX。
- 中文说明和数学公式分开组织。正文中使用行内 `MathFormula`；SVG 内使用 `foreignObject` 封装的 `SvgFormula`。
- JSX 属性中的反斜杠容易被转义。包含 LaTeX 命令时优先使用表达式字符串：

```tsx
<MathFormula latex={"\\nabla f\\perp t"} />
```

不要写：

```tsx
<MathFormula latex="\nabla f\perp t" />
```

- KaTeX 配置应 `throwOnError: false` 以避免整页崩溃，但交付前页面中的 `.katex-error` 数量必须为 0。
- 公式字号、基线和颜色应由统一组件控制，不要在业务 JSX 中到处拼局部样式。
- 行内公式容器必须保留横向排版：为 KaTeX 外层设置 `display: inline-block` 与 `white-space: nowrap`。窄卡片中的长公式应扩大列宽、缩小字号或允许横向滚动，绝不能让公式按字符挤成竖排。这个问题要在普通桌面、响应式窄屏和录屏模式分别检查。

## 7. 数学与几何正确性（硬性要求）

### 7.1 二维坐标

- 当数学坐标的 x/y 单位相同时，必须使用同一个屏幕比例尺。
- 推荐：

```ts
const PLOT_SCALE = VIEW_H / (MAX - MIN);
const PLOT_W = (MAX - MIN) * PLOT_SCALE;
const PLOT_X = (VIEW_W - PLOT_W) / 2;
```

- 坐标转换、指针反变换、曲线、箭头、标签和命中测试必须共享同一套 `sx/sy`。
- 不允许分别按 SVG 宽高独立缩放 x/y；这会把法向、切向和真实角度画错。

### 7.2 三维投影

- 明确区分数学空间中的向量和它在屏幕上的投影。
- 同一个 `project3d(x, y, z)` 必须用于曲面网格、坐标轴、等高截线、粒子、投影线和向量。
- 如果要把梯度画在定义域中，应画在 `z=0` 平面，而不是贴在函数曲面上；曲面点可用竖直投影线连接。
- 教学视角必须说明优先目标。若要求等高截面不倾斜，圆的投影应为水平椭圆，左右极值点的屏幕 y 值必须相等。
- 2D 与 3D 同步视图中，同一点、同一函数值、同一等高线和同一向量必须同步更新。

### 7.3 向量与约束

- 箭头起点、方向和长度都必须有数学定义；不要只为“看起来合适”手调到失真。
- 若 `lambda = 1` 表示等比例作用，两个向量必须使用相同屏幕比例尺。
- 不等式约束的支持力仅在接触边界时出现；离开边界时对应乘子应归零。
- 法向锥、可行域、切线、法线等集合或方向应从定义实时计算，不要写死在某个特殊点。
- 数学量接近零时，短箭头与标签应一起隐藏，使用同一阈值，避免状态文案和图形不同步。

## 8. 交互与状态

- 拖动应使用 Pointer Events，并支持 `setPointerCapture`，确保拖出元素后仍连续更新。
- 指针的屏幕坐标必须通过当前 SVG 的实际边界和同一坐标变换映射回数学空间。
- 对不可行拖动要采用明确策略：投影回可行域、截断或拒绝；策略必须与教学文案一致。
- 所有派生量应由一个源状态计算，不要分别维护会互相漂移的副本。
- 推荐公开可语义查询的 DOM 标记，如 `data-role="draggable-point"`、`data-role="objective-contours"`，便于测试。
- 状态面板只展示当前教学关系需要的数据，不要为填满空间增加指标。

## 9. 文案与教学节奏

- 每一步只回答一个问题：对象是什么、方向意味着什么、为什么成立。
- 使用“先对象，再变化，最后结果”的顺序；命名和公式仅在必要时出现。
- 图上只保留必要的短标签；较长解释放在文档或讲稿中，不默认放入画布、画布下方或状态面板。
- 避免误导性符号。例如“可行域”和 `g(x)\le0` 之间使用正常排版间距，不加乘法点。
- 物理类比必须指出其数学对应关系。例如“墙的支持力”对应边界法向与非负乘子，不能让类比代替定义。

## 10. 实现约定

- React/TypeScript 组件保持职责清晰：公式、箭头、坐标投影、Scene 注册、纯数学计算分别封装。
- 复杂数学计算优先写成无副作用纯函数，并可用数值测试验证。
- SVG `viewBox` 是数学绘图坐标；CSS 负责屏幕适配。不要在多个层级重复引入互相冲突的缩放。
- 新功能优先复用现有 `MathFormula`、`SvgFormula`、`Arrow`、Scene registry 和录屏布局。
- 不要引入重量级 3D 库来完成简单函数曲面；简单教学曲面优先用确定性的 SVG 投影。确实需要旋转、遮挡和复杂曲面时再评估 WebGL。
- 修改现有页面时，保留用户未要求更改的交互、数学状态和视觉语义。

## 11. 验收标准

每次交付至少完成以下检查：

### 自动检查

- lint 通过；
- production build 通过；
- `git diff --check` 无空白错误；
- 页面 `.katex-error` 数量为 0；
- 控制台无新增运行时错误。

### 数学检查

- 用数值验证关键几何关系，而不只凭截图：
  - 垂直关系检查点积接近 0；
  - 等比例向量检查屏幕长度；
  - 2D/3D 同向检查屏幕 `dx/dy` 符号；
  - 水平椭圆检查左右极值点 y 差为 0；
  - KKT 检查残差、互补松弛和乘子非负性。
- 拖动至少检查边界点、内部点、交点和接近零的退化状态。

### 视觉检查

- 普通桌面视口检查一次；
- 精确 1920×1080 录屏模式检查每个 Scene 和所有内部步骤；
- `scrollHeight === clientHeight`；
- 画布和需要的检查器均在视口内；默认无标题、说明文字及任何位置的导航栏、步骤按钮或页码；
- 标签不重叠、不出界，箭头尖端与线宽协调；
- 键盘能跨步骤、跨 Scene 前后切换，`Escape` 能退出录屏模式。

### 服务检查

- 打印 PID 或启动日志不代表成功；必须同时验证端口监听和页面可访问。
- 启动失败时先检查当前 Node/npm/pnpm 路径和现有锁文件，不要盲目重装依赖或切换包管理器。

## 12. 完成定义

一个 Scene 只有在以下条件全部满足时才算完成：

1. 教学目标单一且清楚；
2. 数学对象、状态和视觉编码一致；
3. 所有公式由 KaTeX 渲染且无错误；
4. 交互在关键边界状态下正确；
5. 1920×1080 内无需滚动；
6. 键盘导航完整；
7. 自动、数值和视觉验收均通过。
